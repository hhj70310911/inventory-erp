import React from 'react';
export default function WorkspaceLoading({error='',retry}:{error?:string;retry?:()=>void}){
 return <main className="workspace-loading"><section className="loading-card" aria-labelledby="loading-title"><div className="loading-brand"><span className="erp-mark" aria-hidden="true">庫</span><span>庫務 ERP<small>INVENTORY WORKSPACE</small></span></div><div className="loading-copy"><span className={error?'loading-error-icon':'loading-spinner'} aria-hidden="true">{error?'!':null}</span><h1 id="loading-title">{error?'暫時無法載入工作區':'正在準備您的工作區'}</h1>{error?<p role="alert">{error}</p>:<p role="status">正在讀取庫存、訂單與帳款資料，請稍候…</p>}</div>{error?<div className="loading-actions"><button type="button" onClick={retry}>重新讀取</button><a href="/login">返回登入</a></div>:<div className="loading-skeleton" aria-hidden="true"><div className="loading-metrics">{[0,1,2].map(i=><div key={i}><span/><strong/><span/></div>)}</div><div className="loading-rows">{[0,1,2].map(i=><div key={i}><span/><span/><span/></div>)}</div></div>}<footer>庫存有序 · 帳務清晰 · 營運安心</footer></section></main>;
}
